{% extends 'base.html.twig' %}

{% block title %}Ajouter un module{% endblock %}

{% block body %}

<a class="retour" href="{{ path('show_one_session', {'id':session.id}) }}">Retour</a>

<div class="example-wrapper">
    <h1 class="centrer" >Ajouter un module à la Session {{ session.intitule }}</h1>

    {% for message in app.flashes('success') %}
    <div class="success">
        {{ message }}
    </div>
    {% endfor %}
    {% for message in app.flashes('error') %}
    <div class="error">
        {{ message }}
    </div>
    {% endfor %}

    <div class="boiteform">
        {{ form(ajoutmoduleform) }} 
    </div>


</div>
{% endblock %}
