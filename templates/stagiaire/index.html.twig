{% extends 'base.html.twig' %}

{% block title %}Hello StagiaireController!{% endblock %}

{% block body %}


<div class="example-wrapper">
   <h1 class="centrer">Liste des stagiaires</h1>
    <table>
        <thead>
            <th>Nom Prénom</th>
            <th>DateNaissance</th>
            <th>Nombre de sessions inscrits</th>
            <th>Actions</th>
         
        </thead>

        {% for stagiaire in stagiaires %}

        <tr>
                <td>
                    <a class="bold" href="{{ path('showOne_stagiaire', {'id':stagiaire.id}) }}">{{ stagiaire.nom }} {{ stagiaire.prenom }}</a>
                </td>

                <td>
                    {{ stagiaire.DateNaissance|date('d/m/Y') }}
                </td>
                <td>
                    {{ stagiaire.sessions|length}}
                </td>
            
                <td>
                    <a class="action" href="{{ path('edit_stagiaire', {'id':stagiaire.id}) }}">&expectation;</a>
                    <a class="action" href="{{ path('remove_one_stagiaire', {'id':stagiaire.id}) }}">&xotime;</a>
                </td>

                    
        {% endfor %}
    </table>
    <div class="centrer">
    <a  href="{{ path('form_stagiaire') }}" >Ajouter un stagiaire</a>
 
    </div>
</div>
{% endblock %}
