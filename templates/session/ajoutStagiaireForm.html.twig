{% extends 'base.html.twig' %}

{% block title %}Liste des sessions{% endblock %}

{% block body %}

<a class="retour" href="{{ path('show_one_session', {'id':session.id}) }}">Retour</a>

<div class="example-wrapper">
    <h1 class="centrer" >Ajouter un stagiaire à la session {{ session.intitule }}</h1>

    


    <div class="boiteform">
        <form action="{{ path('ajout_stagiaire', {'id':session.id}) }}" method="post">
            <div>
                <select name="stagiaire">
                    {% for stagiaire in stagiairesDispo %}
                        <option value="{{ stagiaire.id }}">{{ stagiaire.fullName }}</option>
        
                    {% endfor %}
                </select>
                <div>
                    <input class="submit" type="submit" value="Ajouter">
                </div>
            </div>
        </form>
    </div>


    {# <div class="boiteform">
        {{ form(ajout_stagiaire_form) }}
    </div> #}

</div>
{% endblock %}
